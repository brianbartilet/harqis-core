## Commands
```
docker exec -it elasticsearch bin/elasticsearch-reset-password -u elastic -i
```
```
docker exec -it elasticsearch /usr/share/elasticsearch/bin/elasticsearch-reset-password -u kibana_system -b
```